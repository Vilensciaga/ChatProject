import { FirectiveDirective } from './firective.directive';

describe('FirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
